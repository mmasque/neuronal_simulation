function kl_div = kl_div_between_sequences(seq_1, seq_2, L, alphabet)
% calculates kl divergence between sequences, computing difference in
% frequency of subsequences of length L

    for i = 1:length(alphabet)^L
        
    end
end